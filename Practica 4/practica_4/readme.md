# Cómo ejecutar el programa

El programa no espera ningún argumento:

```
java -jar camino_caballo.jar 
```

El programa funciona de forma completamente interactiva. Para utilizarlo, solo hace falta ejecutarlo y seguir las instrucciones que se imprimen en la consola.
